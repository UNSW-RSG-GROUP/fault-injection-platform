######################################################
#
# XPS Tcl API script generated by PlanAhead
#
######################################################

proc _main_ {} {
  cd "D:/Projects/RSG/LP14/fie/base/mb_sys"
  if { [ catch {xload xmp mb_sys.xmp} result ] } {
    exit 10
  }

  # Set host application type
  xset intstyle PA

  # Reset target (cleanup generated files)
  run clean

  return $result
}

if { [catch {_main_} result] } {
  exit -1
}

# Check return status and exit
if { [string length $result] == 0 } {
  exit 0
} else {
  exit $result
}
